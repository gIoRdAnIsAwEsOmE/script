//github.com/showcases
$$('.exploregrid-item-title.mb-2').forEach((title) => { console.log(title.innerHTML);});
//https://www.cnet.com/news/
$$('.assetHed').forEach((title) => {
  console.dir(title.children[1].innerText);
});